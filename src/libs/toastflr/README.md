# dependencies
- tailwind
- redux

# configuration
1. add `toastflrSlice` to your "store x"
2. add `<ToastrManager/>` to your app, inside the provider with your "store x"

<hr>

<i>NOTE: this setup wasn't tested</i>